#include <iostream>
using namespace std;

struct node{
    int nilai;
    node* next;
};

int main(){
    node *node1 = new node();
    node1->nilai = 70;
    node1->next = nullptr;

    node *node2 = new node();
    node2->nilai = 75;
    node2->next = nullptr;

    node *node3 = new node();
    node3->nilai = 60;
    node3->next = nullptr;

    node *node4 = new node();
    node4->nilai = 90;
    node4->next = nullptr;

    node *node5 = new node();
    node5->nilai = 89;
    node5->next = nullptr;

    node *node6 = new node();
    node6->nilai = 77;
    node6->next = nullptr;

    node *node7 = new node();
    node7->nilai = 40;
    node7->next = nullptr;

    node *node8 = new node();
    node8->nilai = 75;
    node8->next = nullptr;

    node *node9 = new node();
    node9->nilai = 97;
    node9->next = nullptr;

    node *node10 = new node();
    node10->nilai = 94;
    node10->next = nullptr;

    //HEAD & TAIL
    node *head = node1;
    node *tail = node10;

    //POINTER
    node1->next=node2;
    node2->next=node3;
    node3->next=node4;
    node4->next=node5;
    node5->next=node6;
    node6->next=node7;
    node7->next=node8;
    node8->next=node9;
    node9->next=node10;

    //LOOP OUTPUT AWAL
    cout << "Output nilai mahasiswa awal : \n";
    node *temp = head;
    while (temp != nullptr)
    {
        cout << temp->nilai << " ";
        temp = temp->next;
    }

    //TAMBAH NEW NODE DI DEPAN
    node *node11 = new node();
    node11->nilai = 100;
    node11->next = nullptr;
    node11->next = head;
    head = node11;//NODE 11 KUDETA POSISI HEAD DARI NODE 1

    cout << "\nnilai mahasiswa setelah ditambahkan node baru di depan: \n";
    temp = head;
    while (temp != nullptr)
    {
        cout << temp->nilai << " ";
        temp = temp->next;
    }

    //TAMBAH NEW NODE DI BELAKANG
    node *node12 = new node();
    node12->nilai = 12;
    node12->next = nullptr;
    node10->next = node12; //MENGKUDETA TAIL DARI NODE 10
    tail = node12;

    cout << "\nnilai mahasiswa setelah ditambahkan node baru di belakang: \n";
    temp = head;
    while (temp != nullptr)
    {
        cout << temp->nilai << " ";
        temp = temp->next;
    }
    
    node *node13 = new node();
    node13->nilai = 0;
    node13->next = node7; // NODE 13 KUDETA POSISI NODE 7 (NODE 7 SEKARANG BERADA DI URUTAN 9 KARENA +1 NODE DIAWAL DAN DIKUDETA NODE 13)
    node6->next =  node13;

    cout << "\nnilai mahasiswa setelah ditambahkan node baru di antara node 6 dan 7: \n";
    temp = head;
    while (temp != nullptr)
    {
        cout << temp->nilai << " ";
        temp = temp->next;
    }

    //HAPUS NODE KE 7 (40)
    temp = head;
    while(temp ->next->nilai!= 40){
        temp = temp ->next;
    }

    node* hapus = temp -> next;
    temp ->next = hapus -> next;
    delete hapus;

    cout << "\nnilai mahasiswa setelah node ke-7 (40) dihapus: \n";
    temp = head;
    while (temp != nullptr)
    {
        cout << temp->nilai << " ";
        temp = temp->next;
    }

}
